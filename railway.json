# Crear el contenido EXACTO
$json = @'
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn calculwinilang_django.wsgi --bind 0.0.0.0:$PORT"
  }
}
'@

# Guardar SIN caracteres adicionales
[System.IO.File]::WriteAllText("$PWD\railway.json", $json, [System.Text.Encoding]::UTF8)